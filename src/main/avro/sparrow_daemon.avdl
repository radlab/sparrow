@namespace("edu.berkeley.sparrow.avro.server")

protocol SparrowDaemonProtocol {
  import idl "sparrow_types.avdl";

  boolean registerFrontend(string app);
 
  boolean registerBackend(string app);
 
  boolean reportLoad(string app, edu.berkeley.sparrow.avro.LoadSpec load); 

  boolean submitJob(array<edu.berkeley.sparrow.avro.TaskSpec> tasks);

}
